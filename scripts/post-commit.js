// const exec = require('child_process').exec

// // 提交开发日志,跳过钩子，不然会死循环
// exec(`git add develop-note.md && git commit --amend --no-verify`, (error) => {
//     process.exit(1)
// })

// process.exit(0)
