// const fs = require('fs')

// const util = require('./util')

// const msgPath = (process.env.GIT_PARAMS).split(' ')[0]
// const author = process.env.GIT_AUTHOR_NAME

// const msg = fs.readFileSync(msgPath, 'utf-8').trim()

// const log = `\n\n### ${util.FormatDate2CN()}@${author} - ${msg}`

// fs.appendFileSync('./develop-note.md', log, 'utf-8')

// process.exit(0)
